Planned UX upgrade per user request (not fully implemented):
1) Cover Image URL field -> replace/add browse control input type=file + allow multiple files.
2) Show preview pane under the browse control with draggable thumbnail list.
3) Always use first image as cover: set banquet_img field to first selected image URL (or keep File->upload logic if exists).
4) Add high quality image render settings (done partially: decoding/loading/object-fit).

Current codebase limitations:
- Existing implementation uses only URL string from Firebase and sets img preview accordingly.
- No upload handler exists in admin_dashboard.js for file->Firebase storage.

